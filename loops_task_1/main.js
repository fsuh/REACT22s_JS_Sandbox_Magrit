for (let odds = 1; odds <= 100; odds = odds ++) {
    if (odds % 3 > 0);
    console.log(odds);
}

for (let evens = 1; evens <= 100; evens = evens ++) {
    if (evens % 2 == 0);
    console.log(evens);
}

function speeds(){
    let dist = Number(prompt("What is the distance?"));
    let times = Number(prompt(""))
}