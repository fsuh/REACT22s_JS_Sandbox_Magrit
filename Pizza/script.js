const order = () => {
    const customerName = document.querySelector('#customerName').value;
    const pizzaSize = document.querySelectorAll("input.pizzaSizes").value;
    const toppings = document.querySelectorAll("input.toppings").value;
    const delivery = documents.querySelectorAll("input.delivery").value;
}
